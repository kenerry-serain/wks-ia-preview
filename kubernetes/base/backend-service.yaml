apiVersion: v1
kind: Service
metadata:
  name: workshop-backend
  labels:
    app: workshop-backend
    version: v1.0
    environment: production
    managed-by: kubectl
spec:
  type: ClusterIP
  selector:
    app: workshop-backend
  ports:
    - port: 80
      targetPort: 8080
      protocol: TCP
